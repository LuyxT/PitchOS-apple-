[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "sh -lc 'npx prisma migrate deploy --schema prisma/schema.prisma && npm run start:prod'"
