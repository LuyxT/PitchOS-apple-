[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "sh -lc 'npm --prefix backend run prisma:generate && npm --prefix backend run migrate:deploy && npm --prefix backend run start:prod'"
