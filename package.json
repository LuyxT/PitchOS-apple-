{
  "name": "pitchinsights-monorepo",
  "private": true,
  "version": "1.0.0",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "build": "npm --prefix backend ci && npm --prefix backend run build",
    "start": "npm --prefix backend run start:prod"
  }
}
