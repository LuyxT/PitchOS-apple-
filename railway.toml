[build]
builder = "NIXPACKS"
buildCommand = "npm ci && npm run build"

[deploy]
startCommand = "sh -lc 'npx prisma migrate deploy --schema backend/prisma/schema.prisma && npm run start:prod --prefix backend'"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
