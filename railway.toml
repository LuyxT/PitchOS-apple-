[build]
builder = "NIXPACKS"
buildCommand = "sh -lc 'cd backend && npm ci && npm run build'"

[deploy]
startCommand = "sh -lc 'cd backend && npm run start:prod'"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
