# ── Required ─────────────────────────────────────────────
DATABASE_URL=postgresql://user:password@localhost:5432/pitchinsights?schema=public

# Option A: single shared secret
JWT_SECRET=your-secret-key-at-least-32-characters-long

# Option B: separate secrets (takes priority over JWT_SECRET)
# JWT_ACCESS_SECRET=your-access-secret
# JWT_REFRESH_SECRET=your-refresh-secret

# ── Optional ─────────────────────────────────────────────
NODE_ENV=development
PORT=3000
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=30d
CORS_ORIGINS=*
